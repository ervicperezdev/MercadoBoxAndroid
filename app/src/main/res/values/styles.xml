<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>


    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
    <style name="RaisedButton_profile" parent="Theme.AppCompat.Light">
        <item name="colorButtonNormal">@color/colorPrimary</item>
        <item name="colorControlHighlight">@color/colorPrimary2</item>
        <item name="android:background">@drawable/corner_round_green</item>
        <item name="android:textColor">@color/colorWhite</item>
    </style>

    <style name="RaisedButton_login" parent="Theme.AppCompat.Light">
        <item name="colorButtonNormal">@color/colorWhite</item>
        <item name="colorControlHighlight">@color/colorWhite2</item>
        <item name="android:background">@drawable/corner_round_white</item>
        <item name="android:textColor">@color/colorPrimary</item>
    </style>
</resources>
